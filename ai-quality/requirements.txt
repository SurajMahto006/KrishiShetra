# KrishiShetra AI Quality Service — Python Dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Python >= 3.10 required.
# For GPU training: install the correct torch CUDA build first.
# See: https://pytorch.org/get-started/locally/

# ── Web framework ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # multipart/form-data uploads

# ── Pydantic (FastAPI uses v2) ─────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1       # BaseSettings for .env loading

# ── AI / Vision ────────────────────────────────────────────
ultralytics==8.2.18            # YOLOv8 — includes torch requirement
torch>=2.0.0                   # installed by ultralytics; pin floor only
torchvision>=0.15.0
opencv-python-headless==4.9.0.80
Pillow==11.0.0
numpy>=1.24.0

# ── Testing ────────────────────────────────────────────────
pytest==8.2.0
httpx==0.27.0                  # async test client for FastAPI
pytest-asyncio==0.23.6

# ── Utilities ──────────────────────────────────────────────
python-dotenv==1.0.1
PyYAML==6.0.1                  # read data.yaml / training config
